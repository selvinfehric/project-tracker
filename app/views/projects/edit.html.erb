<%= render 'shared/menu' %>
	
<h1>Edit</h1>

<%= render 'form' %>
<!--
<%= link_to 'Show', @project %> |
<%= link_to 'Back', projects_path %>
-->
<h2>Users</h2>
<br/>
<!--
<% @users.each do |user| %>
	<% if (user.username != "")%>
	  <ul>
	    <li><%= user.username %>
		<% if (@project.users.exists?(user))%>
	    	<%= link_to 'Remove from project', '/projects/removeuser/'+user.id.to_s, :class=>"btn btn-secondary btn-mini" %>
		<% else %>
			<%= link_to 'Add to project', '/projects/adduser/'+user.id.to_s, :class=>"btn btn-secondary btn-mini" %>
		<% end %>
	    </li>
	  </ul>				
	<% end %>
<% end %>
-->

<table id="user_list" class="table table-striped">
	<tr>
		<th>Name</th>
		<th>Add/Remove</th>
	</tr>

	<% @users.each do |user| %>
	  <tr>
	    <td><%= user.username %></td>
	    <td>
	    	<% if (@project.users.exists?(user))%>
	    		<%= link_to 'Remove from project', '/projects/removeuser/'+user.id.to_s, :class=>"btn btn-secondary btn-mini" %>
			<% else %>
				<%= link_to 'Add to project', '/projects/adduser/'+user.id.to_s, :class=>"btn btn-secondary btn-mini" %>
			<% end %>
		</td>
	  </tr>
	<% end %>
</table>
